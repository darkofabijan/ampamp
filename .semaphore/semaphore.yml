version: v1.0
name: First pipeline example
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: Build
    task:
      jobs:
      - name: Build job
        commands:
          - checkout
          - echo "build"

  - name: Test
    task:
      prologue:
        commands:
          - checkout

      jobs:
      - name: Unit tests
        commands:
          - echo "Unit"

      - name: Functional tests
        commands:
          - echo "Func"

      - name: Linting
        commands:
          - echo "Linting"
