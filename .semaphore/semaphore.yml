version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: s1-linux-aws
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - checkout
            - ls -lah
